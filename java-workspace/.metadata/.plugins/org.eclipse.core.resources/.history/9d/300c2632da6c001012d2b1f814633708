package com.kh.first; // 패키지 선언부 : 현재 클래스의 위치

	/*
	 *  주석(Comments) : 긴 내용 주석 하고 싶을 경우 /* + Enter
	 *  
	 *  주석은 프로그램을 실행하는데 아무런 영향을 주지 않음!
	 * 	코드를 작성한 후에 작성한 코드를 이해하기 쉽도록 하기 위함.
	 *	주석을 달아놓으면 유지보수 시간을 줄일 수 있음.
	 *	
	 *	주석의 종류(3가지 / 7/29 두가지만)
	 *
	 *	1. 한 줄 주석(//) : 주석 내요이 한 줄 일때 사용
	 *	"//" 이후부터 행의 끝까지 모든 내용을 주석으로 간주
	 * 	2. 여러 줄 주석("/*~~~~*"/) 여러 줄의 주석이 필요할 때 사용
	 * 	"~~~" 사이의 모든 내용이 주석으로 간주
	 * 
	 */

	/*
	 *	패키지(package) :
	 * 
	 * 	하나의 패키지 안에 여러 개의 클래스들이 존재할 수 있음.
	 * 	단, 하나의 패키지 안에 동일한 이름의 클래스는 존재할 수 없음.
	 * 
	 * 	프로그램을 만들 때 모든 클리스들을 동일한 패키지에 생성하여 관리하면 복잡하기 때문에
	 * 	클래스들을 용도에 맞게 구분해서 패키지에 보관하는 것을 권장한다.
	 * 
	 * 	권장사항
	 * 	패키지는 적어도 세 단계 이상으로 만들어주는 것을 권장함.
	 * 	ex) com.kh.first
	 * 
	 *  com.(회사이름).(프로젝트이름/팀이름/카테고리이름)
	 * 
	 */

// 보라색: 예약어(keyword) 의미가 있는 단어들 (이름으로 사용 안됨)
public class FirstClass { // 클래스 이름을 FirstClass라고 지었음. { Scope } : 블록
	
	//	Scope(블록) {} 은 중괄호로 묶은 부분을 이야기하며, 복합문을 구성하는 요소이다.
	// 	즉, 종속된 문장이 다수 개일 떄 '{' 로 시작하고 '}' 로 닫습니다.
	
	//	main method (entry point : 프로그램의 시작점, 프로그램 당 1개는 가지고 있어야함)
	public static void main(String[] args) {//	Run 클릭 시 'console'에 날짜와 출력 내용이 찍히면 잘 돌아가는 것.
		// static,dynamic, auto ( 3개는 항상 따라다님 ) // void : return type  // (String~) : 매개변수, 파라미터
		
	}
	
	
}
